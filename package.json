{
  "private": true,
  "name": "@axonivy/variable-editor-root",
  "engines": {
    "node": ">=24.11"
  },
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "check": "pnpm run type && (pnpm run lint:ci || true) && pnpm run test:ci",
    "clean": "pnpm run -r clean",
    "build": "tsc --build",
    "package": "pnpm run -r package",
    "build:clean": "pnpm run -r build:clean",
    "type": "pnpm run -r type",
    "lint": "eslint",
    "lint:ci": "eslint -o eslint.xml -f checkstyle",
    "lint:fix": "eslint --fix",
    "lint:inspect": "eslint --inspect-config",
    "generate": "pnpm run --filter @axonivy/variable-editor-protocol generate",
    "dev": "pnpm run --filter @axonivy/variable-editor-standalone dev",
    "test": "pnpm run --filter @axonivy/variable-editor test",
    "test:ci": "pnpm run -r test:ci",
    "webtest": "pnpm run --filter @axonivy/variable-editor-playwright webtest",
    "webtest:screenshot": "pnpm run --filter @axonivy/variable-editor-playwright webtest:screenshots",
    "update:axonivy:next": "pnpm update -r @axonivy",
    "raise:version": "lerna version --allow-peer-dependencies-update --no-git-tag-version --no-push --ignore-scripts --exact --yes",
    "publish:next": "lerna publish --exact --canary --preid next --pre-dist-tag next-12.0.16 --no-git-tag-version --no-push --ignore-scripts --yes"
  },
  "devDependencies": {
    "@axonivy/eslint-config": "next-12.0.16",
    "@lerna-lite/cli": "^4.11.5",
    "@lerna-lite/publish": "^4.11.5",
    "@lerna-lite/version": "^4.11.5",
    "@types/node": "^24.13.0",
    "rimraf": "^6.1.3",
    "typescript": "^5.9.3"
  }
}
