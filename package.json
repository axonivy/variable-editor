{
  "private": true,
  "name": "@axonivy/variable-editor-root",
  "engines": {
    "node": ">=20.19.0"
  },
  "volta": {
    "node": "22.17.0"
  },
  "scripts": {
    "ci": "pnpm install && pnpm package && (pnpm lint:ci || true) && pnpm type && pnpm test:ci && pnpm i18n:ci",
    "clean": "pnpm -r clean",
    "build": "tsc --build && tscp --build",
    "package": "pnpm -r package",
    "build:clean": "pnpm -r build:clean",
    "type": "pnpm -r type",
    "lint": "eslint",
    "lint:ci": "eslint -o eslint.xml -f checkstyle",
    "lint:fix": "eslint --fix",
    "lint:inspect": "eslint --inspect-config",
    "i18n:extract": "lerna run i18n:extract",
    "i18n:ci": "lerna run i18n:ci",
    "generate": "pnpm -r generate",
    "dev": "pnpm --filter @axonivy/variable-editor-standalone dev",
    "test": "pnpm -r test",
    "test:ci": "pnpm -r test:ci",
    "webtest": "pnpm --filter @axonivy/variable-editor-playwright webtest",
    "webtest:screenshot": "pnpm --filter @axonivy/variable-editor-playwright webtest:screenshots",
    "update:axonivy:next": "pnpx --yes npm-check-updates @axonivy* -ws -t semver -u",
    "publish:next": "lerna publish --exact --canary --preid next --pre-dist-tag next --no-git-tag-version --no-push --ignore-scripts --yes"
  },
  "devDependencies": {
    "@axonivy/eslint-config": "~13.2.0-next.780",
    "@axonivy/prettier-config": "~13.2.0-next.780",
    "@lerna-lite/cli": "^4.0.0",
    "@lerna-lite/publish": "^4.0.0",
    "@lerna-lite/run": "^4.0.0",
    "@lerna-lite/version": "^4.0.0",
    "@types/node": "^22.10.7",
    "prettier": "^3.6.2",
    "rimraf": "^6.0.1",
    "typescript": "~5.8.0",
    "typescript-cp": "^0.1.9"
  },
  "prettier": "@axonivy/prettier-config"
}
